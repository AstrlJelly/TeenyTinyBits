{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Clean",
            "type": "shell",
            "command": "rm -r ${workspaceFolder}/build ${workspaceFolder}/.cache",
            "problemMatcher": []
        },
        {
            "label": "Build (Debug)",
            "type": "shell",
            "command": "mkdir -p ${workspaceFolder}/build && /usr/bin/cmake --build ${workspaceFolder}/build",
            "problemMatcher": []
        }
    ],
}