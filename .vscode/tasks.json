{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake (Debug)",
            "type": "shell",
            "command": "/usr/bin/cmake -S ${workspaceFolder} -B ${workspaceFolder}/build -D CMAKE_BUILD_TYPE=Debug",
            "problemMatcher": []
        },
        {
            "label": "Build (Debug)",
            "type": "shell",
            "command": "/usr/bin/cmake --build ${workspaceFolder}/build",
            "problemMatcher": []
        }
    ],
}